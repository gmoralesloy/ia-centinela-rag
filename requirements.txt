# Dependencias principales del RAG
langchain==0.2.4
langchain-core==0.2.4
langchain-community==0.2.4
pydantic==2.7.4 # Versión estable para compatibilidad
chromadb==0.4.18 # Versión estable de la base de datos vectorial
sentence-transformers==2.7.0
pypdf

# Dependencias para la API Web (reemplazando a Streamlit)
fastapi==0.111.0
uvicorn==0.30.1

# Dependencias para el LLM Open Source (Llama3/Mixtral)
# Estas librerías son las que se encargarán de usar la GPU de GCP.
torch==2.3.1
transformers==4.42.3
accelerate==0.31.0